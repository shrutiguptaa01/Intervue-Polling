{
  "name": "assignment",
  "version": "1.0.0",
  "description": "Intervue Poll Assignment with Frontend and Backend",
  "main": "backend/server.js",
  "scripts": {
    "start": "node server.js",
    "server": "nodemon server.js",
    "client": "cd intervue-poll-frontend && npm run dev",
    "install-all": "npm install && cd intervue-poll-frontend && npm install"
  },
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "socket.io": "^4.8.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1"
  }
}
